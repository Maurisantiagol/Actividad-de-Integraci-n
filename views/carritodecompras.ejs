<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/mastersyle.css">
    <link rel="stylesheet" href="css/formularios.css">
    <link rel="shortcut icon" href="assets/svg/logo.svg">
    <title>Mis compras</title>
    <style>
        label {
            height: fit-content;
        }
    </style>
</head>

<body>
    <nav>
     <a>Carrito de compras</a> 
    </nav>
    <div class="principal">
        <header>
            <h1>Carrito de compras</h1>
            
        </header>
        <div class="col-md-7">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <td>n°</td>
                        <td>Productos</td>
                        <td>Descripcion</td>
                        <td>Cantidad</td>
                        <td>Acciones</td>
                    </tr>
                </thead>
                <tbody>
                    <% if (data) { %>
                        <% for(var i = 0; i < data.length; i++) { %>
                            <tr>
                                <td><%= (i + 1) %></td>
                                <td><%= data[i].nom_pro %></td>
                                <td><%= data[i].desc_pro %></td>
                                <td><%= data[i].cant_pro %></td>
                                <td>
                                    <a href="/update/<%= data[i].id_pro %>" class="btn btn-info">Editar</a>
                                    <a href="/delete/<%= data[i].id_pro %>" class="btn btn-danger">Borrar</a>
                                </td>
                            </tr>
                            <% } %>
                            <% } %>
                </tbody>
            </table>
        </div>
        <div class="col-md-5">
            <div class="card-body">

                <form action="/add" method="POST">
                    <div class="form-group">
                        <input type="text" name="nom_pro" placeholder="Nombre" class="form-control" autofocus>
                    </div>
                    <div class="form-group">
                        <input type="text" name="desc_pro" placeholder="Descripcion" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="text" name="cant_pro" placeholder="Cantidad" class="form-control">
                    </div>
                    <input class="b" type="submit" value='Crear'>
                    
                </form>
            </div>
        </div>
    </div>
    <!--Cada grupo debe tener su propio menu
    para eso es necesario que cada input tenga un id diferentes
    los for del label deben coincidir con el del input-->
    <div class="grid">

    </div>
    <br>
    <footer>
        <%- include('partials/footer') %>
    </footer>
</body>

</html>